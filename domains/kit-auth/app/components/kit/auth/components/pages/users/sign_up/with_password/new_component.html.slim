= render Kit::Auth::Components::Forms::SignupFormComponent.new(\
  model:       model,
  csrf_token:  csrf_token,
  route_id:    'web|users|new',
  errors_list: errors_list,
)

.text-center.mt-3
  ' Already have an account?
  = router_link_to(id: 'web|users|sign_in')
    b Sign in!