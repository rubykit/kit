= render Kit::Auth::Components::Forms::ResetPassword.new(** \
  model:             @model,
  csrf_token:        form_authenticity_token,
  auth_token_secret: @auth_token_secret,
  errors_list:       @errors_list,
)