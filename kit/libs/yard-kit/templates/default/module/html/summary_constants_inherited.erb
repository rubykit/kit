<% toplist = Yard::Kit::Services::Modules.get_inherited_constants(object: object, options: options) %>
<% toplist.each do |_superclass_name, data| %>
  <%
     superclass = data[:superclass]
     list       = data[:list]
  %>

  <div class="summary-inherited-constants-<%= superclass.name.downcase %> summary summary-inherited">
    <h3><%= superclass.type == :class ? 'Inherited' : 'Included' %> from <%= linkify(superclass) %></h3>
    <% list.each do |item| %>
      <%= yieldall item: item, object: object, superclass: superclass %>
    <% end %>
  </div>

<% end %>
