<%
  list = Yard::Kit::Services::Modules.get_object_constants({
    object:          object,
    options:         options,
    verifier_runner: ->(el) { run_verifier(el) },
  })
%>
<% if list.size > 0 %>

  <div class="summary-constants summary">
    <h2>
      <a href="#constants">Constants</a>
    </h2>

    <% list.each do |item| %>
      <%= yieldall item: item %>
    <% end %>
  </div>

<% end %>