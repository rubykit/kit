<%
  # Note: seems that including Helper in layout/html/setup.rb does not do the trick.
  config = Yard::Kit::Config.config
%>

<h1>
  <small class="app-vsn"><%= config[:project] %> v<%= config[:version] %></small>
  API Reference
</h1>

<%= yieldall %>
